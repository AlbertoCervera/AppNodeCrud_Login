extends layout 

block content
    .container 
        h1 Login
        .formulario 
            form(action="/login", method="post") 
                label Username 
                input(type="text", name="username")
                label Password
                input(type="password", name="password")
                input(type="submit", value="Entrar")
            span 
                a(href="/register") No tienes cuenta?